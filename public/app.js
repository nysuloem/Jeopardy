const socket=io(),app=document.querySelector('#app');let state=null,roomCode=null,myId=null,isDisplay=false,introRun=false,lastSpokenKey='';
const paths=location.pathname.split('/').filter(Boolean);const saved=JSON.parse(localStorage.getItem('jeopardySession')||'null');
if(paths[0]==='host'&&paths[1])watchRoom(paths[1]);else if(paths[0]==='join'&&paths[1])showJoin(paths[1]);else showLanding();
socket.on('state',next=>{state=next;roomCode=next.code;render();});
socket.on('connect',()=>{if(isDisplay&&roomCode)watchRoom(roomCode);else if(paths[0]==='join'&&saved?.code===roomCode&&saved?.id)socket.emit('rejoin',{code:roomCode,playerId:saved.id},r=>{if(r?.ok)myId=r.playerId;});});

function showLanding(){app.innerHTML=`<main class="page"><section class="landing"><h1 class="logo-word">JEOPARDY!</h1><p class="tagline">Alex Trebek or Ken Jennings. Three contestants. One board.</p><button class="primary" id="create">Host on This Screen</button></section></main>`;document.querySelector('#create').onclick=()=>{unlockSound();socket.emit('createRoom',{},r=>{if(!r?.ok)return toast('Could not create game');roomCode=r.code;isDisplay=true;history.replaceState({},'',`/host/${r.code}`);});};}
function watchRoom(code){roomCode=String(code).toUpperCase();isDisplay=true;socket.emit('watchRoom',{code:roomCode},r=>{if(!r?.ok)return toast(r.error);state=r.room;render();});}
function showJoin(code){roomCode=String(code).toUpperCase();if(saved?.code===roomCode&&saved?.id){app.innerHTML='<main class="phone"><h2>Rejoining your game…</h2></main>';return;}app.innerHTML=`<main class="page"><form class="panel join-form" id="join"><h1>Become a Contestant</h1><label>Your name<input id="name" maxlength="28" autocomplete="name" required></label><label>Occupation<input id="occupation" maxlength="50" placeholder="e.g., biology professor" required></label><label>Where are you from?<input id="location" maxlength="50" placeholder="e.g., London, Ontario" required></label><div><strong>Write your name as it should appear on your podium</strong><div class="signature-box"><canvas id="signature" width="900" height="300"></canvas><div class="signature-tools"><button type="button" class="secondary" id="clear">Clear</button><button type="button" class="secondary" id="typeInstead">Type instead</button></div></div></div><label class="typed-name" id="typedWrap">Podium name<input id="typed" maxlength="28" placeholder="Type your name"></label><button class="primary">Join the Game</button></form></main>`;setupSignature();document.querySelector('#join').onsubmit=e=>{e.preventDefault();unlockSound();const canvas=document.querySelector('#signature');const typed=document.querySelector('#typed').value.trim();const signature=canvas.dataset.drawn==='1'?canvas.toDataURL('image/png'):null;socket.emit('joinRoom',{code:roomCode,name:value('#name'),occupation:value('#occupation'),location:value('#location'),signature,typedSignature:typed},r=>{if(!r?.ok)return toast(r.error);myId=r.playerId;localStorage.setItem('jeopardySession',JSON.stringify({code:roomCode,id:myId}));});};}
function setupSignature(){const c=document.querySelector('#signature'),x=c.getContext('2d');x.strokeStyle='#fff';x.lineWidth=9;x.lineCap='round';let drawing=false;const point=e=>{const r=c.getBoundingClientRect();return[(e.clientX-r.left)*c.width/r.width,(e.clientY-r.top)*c.height/r.height]};c.onpointerdown=e=>{drawing=true;c.setPointerCapture(e.pointerId);x.beginPath();x.moveTo(...point(e));};c.onpointermove=e=>{if(!drawing)return;x.lineTo(...point(e));x.stroke();c.dataset.drawn='1';};c.onpointerup=c.onpointercancel=()=>drawing=false;document.querySelector('#clear').onclick=()=>{x.clearRect(0,0,c.width,c.height);c.dataset.drawn='0';};document.querySelector('#typeInstead').onclick=()=>{document.querySelector('#typedWrap').classList.toggle('active');document.querySelector('#typed').value=value('#name');};}
function render(){if(!state)return;document.body.className=state.era;if(state.phase==='lobby')return renderLobby();if(state.phase==='intro')return renderIntro();introRun=false;if(!isDisplay)return renderPhone();if(state.phase==='board'||state.phase==='round_break')return renderBoard();if(['clue','answer','review','daily_wager','daily_clue','daily_answer','daily_review'].includes(state.phase))return renderClue();if(state.phase.startsWith('final_'))return renderFinal();}
function renderLobby(){if(!isDisplay){app.innerHTML=`<main class="phone"><h1>You’re on Jeopardy!</h1><p>Look at the host screen. Your handwritten name will appear on your podium.</p><div class="era-pill">${eraName()}</div></main>`;return;}const url=`${location.origin}/join/${state.code}`;app.innerHTML=`<main class="page"><section class="lobby-grid"><article class="panel"><h2>Scan to join</h2><div class="join-code">${state.code}</div><img class="qr" src="/api/room/${state.code}/qr"><p>${url}</p><div class="era-pill">${eraName()}</div><p>${state.generating?'OpenAI is preparing a fresh board in the background. You can still start immediately.':state.generated?'Fresh AI-generated board ready.':'Built-in tournament board ready.'}</p></article><article class="panel"><h1>Today’s Contestants</h1><div class="contestant-list">${state.players.map(card).join('')||'<p>Waiting for contestants…</p>'}</div><div class="host-controls"><button class="primary" id="start" ${state.players.length?'':'disabled'}>Start Jeopardy!</button></div></article></section></main>`;document.querySelector('#start').onclick=()=>socket.emit('startGame',{code:state.code},r=>{if(!r?.ok)toast(r.error||'Could not start');});}
function card(p){return `<article class="contestant-card">${signature(p)}<strong>${escape(p.name)}</strong><span class="occupation">${escape(p.occupation)} · ${escape(p.location)}</span></article>`;}
function signature(p){return p.signature?`<img class="signature" src="${p.signature}" alt="${escape(p.name)} signature">`:`<span class="typed-signature">${escape(p.typedSignature||p.name)}</span>`;}
function renderIntro(){if(!isDisplay){app.innerHTML='<main class="phone"><h1>Listen for your introduction!</h1></main>';return;}const bg=logoPath(state.logo);app.innerHTML=`<section class="intro-screen"><img class="intro-bg" src="${bg}"><div class="intro-vignette"></div><div class="intro-content" id="introContent"><h1 class="logo-word">JEOPARDY!</h1></div></section>`;if(!introRun){introRun=true;runIntro();}}
async function runIntro(){let bed=null;if(state.era==='jennings'){bed=new Audio('/assets/ken-introduction-web.mp3');bed.volume=.34;bed.play().catch(()=>{});}await wait(1400);for(const p of state.players){document.querySelector('#introContent').innerHTML=`<div class="intro-card">${signature(p)}<h2>${escape(p.name)}</h2><p>${escape(p.occupation)} from ${escape(p.location)}</p></div>`;await speak(introSentence(p));await wait(350);}const host=state.era==='trebek'?'/assets/alex-trebek-podium.webp':'/assets/ken-jennings-podium.webp';document.querySelector('#introContent').innerHTML=`<img class="intro-host" src="${host}"><div class="host-title">${state.era==='trebek'?'ALEX TREBEK':'KEN JENNINGS'}</div>`;await speak(`And now, here is the host of Jeopardy, ${state.era==='trebek'?'Alex Trebek':'Ken Jennings'}!`);bed?.pause();socket.emit('introFinished',{code:state.code});}
function introSentence(p){const statsText=state.introText;const marker=`${p.name}.`;const start=statsText.indexOf(`A ${p.occupation} from ${p.location}, ${p.name}.`);if(start<0)return `A ${p.occupation} from ${p.location}, ${p.name}.`;const rest=statsText.slice(start);const next=rest.indexOf(' A ',marker.length);const host=rest.indexOf(' And now',marker.length);const end=[next,host].filter(x=>x>0).sort((a,b)=>a-b)[0]??rest.length;return rest.slice(0,end);}
function renderBoard(){const round=state.game.rounds[state.round],vals=state.round===0?[200,400,600,800,1000]:[400,800,1200,1600,2000];const used=(c,r)=>state.used.some(x=>x.round===state.round&&x.category===c&&x.row===r);app.innerHTML=`<main class="game"><div class="board-wrap"><section class="board">${round.categories.map(c=>`<div class="category">${escape(c.name)}</div>`).join('')}${vals.flatMap((v,r)=>round.categories.map((c,ci)=>`<button class="clue-tile ${used(ci,r)?'used':''}" data-c="${ci}" data-r="${r}" ${used(ci,r)||state.phase!=='board'?'disabled':''}>${used(ci,r)?'':money(v)}</button>`)).join('')}</section></div>${status()}${state.phase==='round_break'?`<div class="host-controls"><button class="primary" id="nextRound">${state.round===0?'Start Double Jeopardy':'Go to Final Jeopardy'}</button></div>`:''}</main>`;document.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>socket.emit('selectClue',{code:state.code,category:b.dataset.c,row:b.dataset.r}));document.querySelector('#nextRound')?.addEventListener('click',()=>socket.emit('nextRound',{code:state.code}));}
function renderClue(){const q=state.game.rounds[state.round].categories[state.selected.category].clues[state.selected.row];const isDaily=state.phase.startsWith('daily_');if(state.phase==='daily_wager')return app.innerHTML=`<main class="clue-screen"><div class="daily">DAILY<br>DOUBLE</div>${status()}</main>`;app.innerHTML=`<main class="clue-screen"><section class="clue-panel">${escape(q.clue||'')}</section>${['review','daily_review'].includes(state.phase)?`<div class="review-response">Correct response: What is ${escape(q.response||'')}?</div>`:''}${status()}<div class="host-controls">${['review','daily_review'].includes(state.phase)?`<button class="secondary" id="override">Reverse Ruling</button><button class="primary" id="continue">Continue</button>`:''}${state.phase==='clue'?'<button class="secondary" id="close">No More Buzzes</button>':''}</div></main>`;document.querySelector('#override')?.addEventListener('click',()=>socket.emit('overrideJudgment',{code:state.code}));document.querySelector('#continue')?.addEventListener('click',()=>socket.emit('continueClue',{code:state.code}));document.querySelector('#close')?.addEventListener('click',()=>socket.emit('closeClue',{code:state.code}));if(['clue','daily_clue'].includes(state.phase))speakClueOnce(q.clue,state.phase);}
async function speakClueOnce(text,phase){const key=`${state.code}:${state.round}:${state.selected.category}:${state.selected.row}:${phase}`;if(lastSpokenKey===key)return;lastSpokenKey=key;await speak(text);socket.emit('clueRead',{code:state.code});}
function renderPhone(){const me=state.players.find(p=>p.id===myId);if(!me){app.innerHTML='<main class="phone"><h2>Reconnect by rejoining the room.</h2></main>';return;}let content='<h2>Watch the host screen</h2>';if(state.phase==='daily_wager'&&state.selectorId===me.id){const max=Math.max(state.round?2000:1000,me.score);content=`<h1>Daily Double!</h1><form class="wager-form" id="wager"><label>Your wager (maximum ${money(max)})<input type="number" min="5" max="${max}" required></label><button class="primary">Lock Wager</button></form>`;}else if(state.phase==='clue'){const attempted=state.attempted.includes(me.id);content=`<h2>${attempted?'You already responded to this clue.':'Listen, then buzz in.'}</h2><button class="buzz" id="buzz" ${state.canBuzz&&!attempted?'':'disabled'}>${state.canBuzz&&!attempted?'BUZZ!':'WAIT'}</button>`;}else if(state.phase==='answer'&&state.buzzedId===me.id)content=answerForm('submitAnswer');else if(state.phase==='daily_answer'&&state.selectorId===me.id)content=answerForm('submitAnswer');else if(state.phase==='final_wager'&&me.score>0&&!me.hasFinalWager)content=`<h1>Final Jeopardy</h1><h2>${escape(state.game.final.category)}</h2><form class="wager-form" id="finalWager"><label>Wager up to ${money(me.score)}<input type="number" min="0" max="${me.score}" required></label><button class="primary">Lock Wager</button></form>`;else if(state.phase==='final_wager'&&me.score<=0)content='<h2>You are not eligible for Final Jeopardy.</h2>';else if(state.phase==='final_wager'&&me.hasFinalWager)content='<h2>Your wager is locked. Waiting for the other contestants.</h2>';else if(state.phase==='final_answer'&&me.score>0&&!me.hasFinalAnswer)content=answerForm('finalAnswer','Your Final Jeopardy response');else if(state.phase==='final_answer'&&me.hasFinalAnswer)content='<h2>Your response is locked.</h2>';else if(state.phase==='final_results')content=`<h1>Final Results</h1><div class="winner">${money(me.score)}</div>`;app.innerHTML=`<main class="phone">${signature(me)}<div class="winner">${money(me.score)}</div>${content}</main>`;wirePhone();}
function answerForm(event,label='Your response'){return `<form class="answer-form" id="answer" data-event="${event}"><label>${label}<input autocomplete="off" autofocus required></label><button class="primary">Submit</button></form>`;}
function wirePhone(){document.querySelector('#buzz')?.addEventListener('click',e=>{e.currentTarget.disabled=true;socket.emit('buzz',{code:state.code});});document.querySelector('#wager')?.addEventListener('submit',e=>{e.preventDefault();socket.emit('submitWager',{code:state.code,wager:e.target.querySelector('input').value},r=>{if(!r?.ok)toast(r.error);});});document.querySelector('#finalWager')?.addEventListener('submit',e=>{e.preventDefault();socket.emit('finalWager',{code:state.code,wager:e.target.querySelector('input').value},r=>{if(!r?.ok)toast(r.error);});});document.querySelector('#answer')?.addEventListener('submit',e=>{e.preventDefault();const event=e.target.dataset.event,answer=e.target.querySelector('input').value;e.target.querySelector('button').disabled=true;socket.emit(event,{code:state.code,answer},r=>{if(!r?.ok){toast(r.error||'Try again');e.target.querySelector('button').disabled=false;}});});}
function renderFinal(){if(state.phase==='final_results'){const winner=[...state.players].sort((a,b)=>b.score-a.score)[0];app.innerHTML=`<main class="final-screen"><section class="final-card"><h2>FINAL JEOPARDY!</h2><p class="review-response">${escape(state.game.final.response||'')}</p><div class="contestant-list">${state.players.map(p=>`<article class="contestant-card">${signature(p)}<strong>${escape(p.name)}</strong><p>${escape(p.finalAnswer||'No response')} · ${p.finalCorrect?'CORRECT':'INCORRECT'}</p><h2>${money(p.score)}</h2></article>`).join('')}</div><div class="winner">${escape(winner.name)} WINS!</div></section></main>`;return;}app.innerHTML=`<main class="final-screen"><section class="final-card"><h1>FINAL JEOPARDY!</h1><div class="final-category">${escape(state.game.final.category)}</div>${state.phase==='final_clue'||state.phase==='final_answer'?`<div class="clue-panel">${escape(state.game.final.clue||'')}</div>`:'<p>Contestants are making their wagers.</p>'}${status()}</section></main>`;if(state.phase==='final_clue')speakFinalOnce(state.game.final.clue);}
async function speakFinalOnce(text){const key=`${state.code}:final`;if(lastSpokenKey===key)return;lastSpokenKey=key;await speak(text);socket.emit('clueRead',{code:state.code});}
function status(){return `<footer class="status"><div class="message">${escape(state.message)}</div><div class="scores">${state.players.map(p=>`<div class="score">${signature(p)}<strong>${money(p.score)}</strong></div>`).join('')}</div></footer>`;}
function speak(text){return new Promise(resolve=>{if(!('speechSynthesis'in window))return resolve();speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.92;u.pitch=state?.era==='trebek'?.78:.92;u.onend=u.onerror=resolve;speechSynthesis.speak(u);});}
function unlockSound(){const C=window.AudioContext||window.webkitAudioContext;if(C){window.jAudio=window.jAudio||new C();window.jAudio.resume?.();}}
function logoPath(i){return ['/assets/logo-modern-globe.webp','/assets/logo-modern-lines.webp','/assets/logo-classic-globe.webp','/assets/logo-gold.webp'][i%4];}
function eraName(){return state.era==='trebek'?'Alex Trebek Era':'Ken Jennings Era';}function money(n){return `${Number(n)<0?'−':''}$${Math.abs(Number(n)||0).toLocaleString()}`;}function value(s){return document.querySelector(s).value.trim();}function escape(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}function wait(ms){return new Promise(r=>setTimeout(r,ms));}function toast(text){const t=document.querySelector('#toast');t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2600);}
